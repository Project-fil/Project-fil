//PostgreSQL  script

create database people
people> create schema people
people> create table people.people
        (
        	id bigserial
        		constraint people_pk
        			primary key,
        	first_name text,
        	last_name text,
        	age int,
        	city text
        )

//PostgreSQL  script


//MongoDB  script

use people
db.people.drop()
db.people.insertOne({
    id: NumberLong("1234567890987654321"),
    first_name: "Test",
    last_name: "Test",
    age: NumberInt("23"),
    city: "Test"
})

//MongoDB  script


//Cassandra  script

CREATE KEYSPACE IF NOT EXISTS people
    WITH REPLICATION = {
        'class' : 'SimpleStrategy',
        'replication_factor' : 1
        };
USE people;
CREATE TABLE people(
    id bigint PRIMARY KEY,
    first_name text,
    last_name text,
    age int,
    city text
);

//Cassandra script